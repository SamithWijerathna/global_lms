{
  "name": "global-lms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:server": "npm run build:backend && npm run build:frontend",
    "restart:server": "npm run build:server && pm2 restart ecosystem.config.js"
  }
}
